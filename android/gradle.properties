# Project-wide Gradle settings.

# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.

# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# Specifies the JVM arguments used for the daemon process.
org.gradle.jvmargs=-Xmx4096m -XX:MaxMetaspaceSize=512m -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8

# AndroidX package structure (React Native 요구사항)
android.useAndroidX=true

# React Native 빌드 설정
reactNativeArchitectures=armeabi-v7a,arm64-v8a,x86,x86_64

# New Architecture (TurboModules/Fabric Renderer) 활성화 여부
newArchEnabled=true

# Hermes JavaScript Engine 활성화 여부
hermesEnabled=true

# Flipper 디버깅 라이브러리 버전 설정
FLIPPER_VERSION=0.273.0

# === Gradle 빌드 최적화 설정 ===

# 1. 빌드 캐시 활성화
org.gradle.caching=true

# 2. 병렬 빌드 활성화
org.gradle.parallel=true

# 3. 필요할 때만 프로젝트 구성 (Configuration on Demand)
org.gradle.configureondemand=true

# 4. Gradle Daemon 사용
org.gradle.daemon=true
